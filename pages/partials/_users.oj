

# Defining a function is the same as defining a partial
module.exports = (users = {}) ->
  table style: styleTable, ->
    tbody ->
      for user in users
        tr 'data-id': user.id, ->
          td user.name
          td user.height

styleTable =
  border: '1px solid grey'

styleCell =
  padding: '10px'

